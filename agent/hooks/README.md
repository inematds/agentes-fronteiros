# agent/hooks/ — ganchos de ciclo de vida

Hooks do Claude Code (`settings.json`). Previstos: `PreToolUse` para roteamento por context-mode (já existe via plugin) e `Stop` para lembrar de `FALHAS.md` quando houve correção.
