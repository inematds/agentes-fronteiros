# agent/channels/ — por onde o pedido chega

- **terminal** (Claude Code) — canal principal.
- **Telegram** — `~/projetos/inemaccbot` (instruções podem ser colocadas lá).
- **HTTP** — API do inemavox (porta 8010) para jobs de mídia.

No eve, `channels/` é só da raiz (subagentes não têm). Mesma regra aqui.
