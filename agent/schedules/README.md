# agent/schedules/ — rotinas

- `/loop` e `/schedule` do Claude Code para rotinas de sessão.
- Cron do sistema para jobs longos (render em lote).
- Rotina prevista: `wiki-lint` diário (`python3 tools/wiki_lint.py`) e `inventario` semanal.
